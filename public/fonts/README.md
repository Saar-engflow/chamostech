# Custom Fonts

Please place your "Chewie" font files in this directory with the following names:

- `chewie-regular.woff2` (for regular weight)
- `chewie-bold.woff2` (for bold weight)

The project is already configured to use these files from this location.
